services:
  backend:
    image: ghcr.io/devmuhammed3/openchat:latest
    build:
      context: ../..
      dockerfile: apps/backend/Dockerfile
    ports:
      - "${PORT:-4000}:4000"
    env_file:
      - .env
    restart: unless-stopped
